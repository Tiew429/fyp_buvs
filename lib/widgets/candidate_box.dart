import 'package:flutter/widgets.dart';

class CandidateBox extends StatelessWidget {
  const CandidateBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}